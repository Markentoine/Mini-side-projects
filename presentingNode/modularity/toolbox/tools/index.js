const hammer = require('./hammer');
const screwdriver = require('./screwdriver');
const clamp = require('./clamp');

module.exports = {
    hammer: hammer,
    screwdriver: screwdriver,
    clamp: clamp,
} // the require function will return this object.